name: SakthiAI Quality Gate

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.4.1 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Production source neutrality guard
        shell: bash
        run: |
          set -euo pipefail
          files=(vite.config.ts render.yaml server/_core/index.ts server/_core/llm.ts server/_core/sdk.ts server/_core/oauth.ts server/storage.ts server/_core/storageProxy.ts client/src/const.ts client/src/_core/hooks/useAuth.ts)
          if grep -En -i "forge\.manus\.im|vite-plugin-manus-runtime|manuspre\.computer|manus\.computer|manus-asia\.computer|manuscomputer\.ai|manusvm\.computer|WebDevAuthPublicService|/app-auth|OAUTH_SERVER_URL|types/manusTypes" "${files[@]}"; then
            echo "Forbidden Manus/Forge/WebDev coupling detected in production-critical SakthiAI source."
            exit 1
          fi

      - name: Typecheck
        run: pnpm check

      - name: Tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Deployable artifact neutrality guard
        shell: bash
        run: |
          set -euo pipefail
          if grep -REn -i "forge\.manus\.im|vite-plugin-manus-runtime|manuspre\.computer|manus\.computer|manus-asia\.computer|manuscomputer\.ai|manusvm\.computer|BUILT_IN_FORGE|WebDevAuthPublicService|/app-auth|OAUTH_SERVER_URL|/manus-storage/|/__manus__/" dist; then
            echo "Forbidden Manus/Forge/WebDev marker found in deployable SakthiAI dist output."
            exit 1
          fi

      - name: Built-server behavior smoke
        shell: bash
        run: |
          set -euo pipefail
          PORT=3211 NODE_ENV=production node dist/index.js >/tmp/sakthiai-server.log 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true' EXIT

          healthy=false
          for attempt in $(seq 1 20); do
            if curl --fail --silent --show-error http://127.0.0.1:3211/healthz >/tmp/sakthiai-health.json; then
              healthy=true
              break
            fi
            sleep 1
          done
          if [ "$healthy" != true ]; then
            cat /tmp/sakthiai-server.log
            exit 1
          fi
          cat /tmp/sakthiai-health.json

          curl --fail --silent --show-error http://127.0.0.1:3211/ >/tmp/sakthiai-root.html
          test -s /tmp/sakthiai-root.html

          ready_status=$(curl --silent --output /tmp/sakthiai-ready.json --write-out '%{http_code}' http://127.0.0.1:3211/readyz)
          cat /tmp/sakthiai-ready.json
          if [ "$ready_status" != "503" ]; then
            echo "Expected /readyz to fail closed with 503 when required preview services are absent; got $ready_status"
            exit 1
          fi
